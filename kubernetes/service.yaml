apiVersion: v1
kind: Service
metadata:
  labels:
    app: application-service
  name: application-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 4000
  selector:
    app: application-js